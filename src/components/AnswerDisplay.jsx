
export default function AnswerDisplay({addNote, _handleSubmit}) {

        // const renderToStave = addNote.map((note) => {
        //     return (
        //         <div>
        //             <img src={note.noteImg} alt="" />
        //         </div>
        //     )
        // })

    return (
        <div>
        <h1>AnswerDisplay</h1>
            {/* {renderToStave} */}
        </div>
        // TODO: render imgs - time signature, bar lines, percussion stave
            // background image: stave and barline
            // time signature rendered dynamically
            // notes rendered dynamically in divs
        // TODO: dynamically change time signature, depending on audio player
    )
}